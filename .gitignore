# Dependencies
node_modules/

# Build output
dist/
dashboard/dist/
*.exe

# Personal config (copy config.example.json → config.json and edit)
config.json

# Runtime state and logs
*.log
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Editor cruft
.vscode/
.idea/
*.swp
*.swo

# Local environment overrides
.env
.env.local
config.local.json

# Local audit scratchpads (findings, not part of the repo)
NOTES.md
docs/AUDIT-*.md
.repo-scan/

# Wrangler dev state (worker/) — caches, miniflare, build output
.wrangler/

.vercel
.env*

# launch board local creds + queue (never commit)
tools/launch-board/.data/
*.vsix

# promotion / social kit drafts — kept local, never committed
/promo/
